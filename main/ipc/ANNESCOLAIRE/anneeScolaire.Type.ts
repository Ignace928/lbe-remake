export type AnneeScolaireType = {
    id_annee: string;
    libelle: string;
}

export type AnneeScolaireCreateType = {
    libelle: string;
}

export type AnneeScolaireUpdateType = {
    libelle?: string;
}
